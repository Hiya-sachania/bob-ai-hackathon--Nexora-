# Setup Guide

> **This file is read by the automated evaluation pipeline. Be precise and complete.**

## Prerequisites

## Prerequisites

Before running the application, ensure you have:

- A modern web browser such as Google Chrome, Microsoft Edge, or Firefox
- Visual Studio Code (recommended)
- Live Server extension for VS Code (recommended)

## Environment Variables

No environment variables are required for the current prototype.

The application uses synthetic sample data and runs directly in the web browser.

| Variable  | Required |
|---|---|---|
| `WATSONX_API_KEY` |  NO |
| `WATSONX_PROJECT_ID` | NO |
| `DATABASE_URL`  | NO |
| `SLACK_WEBHOOK_URL` | NO |

## Installation

```bash
# 1. Clone the repository
git clone https://github.com/Hiya-sachania/bob-ai-hackathon--Nexora-.git
cd bob-ai-hackathon--Nexora-

# 2. No dependencies required
The project uses HTML, CSS, and JavaScript and does not require any external packages.

# 3. Configure environment
No environment configuration is required for the current prototype.

# 4. Run the project
Open the `src` folder and double-click `index.html`.
The application will open directly in your web browser.
```

## Running the Application

```bash
## Running the Application

1. Open the `src` folder.
2. Open `index.html` in a web browser.
3. The application will load automatically.

No backend, database, or additional installation is required.

For local development, the project can also be opened using a local server such as VS Code Live Server.

## Running Tests

```bash
[your test command — e.g.: pytest tests/ -v]
```

## Quick Demo (Optional)

If you have a demo script or sample data to showcase the project quickly:

```bash
[e.g.: python demo/seed_demo_data.py]
[e.g.: open http://localhost:8000/demo]
```

## Troubleshooting

| Issue | Solution |
|---|---|
| Website does not open | Website does not open |
| Styling is not showing | Check that `style.css` is in the same `src` folder and that the CSS file is correctly linked in `index.html`. |
|  Buttons or features are not working | Refresh the page and make sure JavaScript is enabled in the browser. |
| CSV export is not working | Check that the browser allows downloads and try the export option again. |
